// simply displays 404 error if user tries to directly access a page that does not exist

import React from 'react';
function Error() {
    return (
        <p>404 ERROR</p>
    );
}
export default Error;